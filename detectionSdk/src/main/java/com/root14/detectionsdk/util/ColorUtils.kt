package com.root14.detectionsdk.util

import android.graphics.Color

object ColorUtils {
    fun getColors(): List<Int> {
        return listOf(
            Color.BLUE,
            Color.GREEN,
            Color.RED,
            Color.CYAN,
            Color.GRAY,
            Color.BLACK,
            Color.DKGRAY,
            Color.MAGENTA,
            Color.YELLOW,
            Color.RED
        )
    }
}
